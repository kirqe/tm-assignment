.container
  .row
    .col-md-7
      %h4

        = @task.id
        = @task.name
      %p
        = @task.description

    .col-md-5
      %h4
        = state(@task)
      %h4
        = @task.user.email
      %h4
        = @task.created_at.strftime('%D')
